# Ratatoskr C package

The repository root `conanfile.py` packages the canonical C library, public
headers, CMake config, and pkg-config metadata. Use `ratatoskr/0.1.0` from
Conan, or consume the installed CMake target `Ratatoskr::ratatoskr`.
